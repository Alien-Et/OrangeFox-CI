name: Build OrangeFox Recovery

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
    - name: Checkout CI repo
      uses: actions/checkout@v4

    - name: Setup build environment
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison build-essential ccache curl flex \
          g++-multilib gcc-multilib git gnupg gperf imagemagick \
          lib32ncurses5-dev lib32readline-dev lib32z1-dev \
          liblz4-tool libncurses5 libncurses5-dev libsdl1.2-dev \
          libssl-dev libxml2 libxml2-utils lzop pngcrush rsync \
          schedtool squashfs-tools xsltproc zip zlib1g-dev

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install repo
      run: |
        sudo curl -s https://storage.googleapis.com/git-repo-downloads/repo > /usr/local/bin/repo
        sudo chmod a+x /usr/local/bin/repo

    - name: Read config
      run: |
        source config.sh
        echo "FOX_BRANCH=$FOX_BRANCH" >> $GITHUB_ENV
        echo "DT_LINK=$DT_LINK" >> $GITHUB_ENV
        echo "DEVICE=$DEVICE" >> $GITHUB_ENV
        echo "OEM=$OEM" >> $GITHUB_ENV
        echo "TARGET=$TARGET" >> $GITHUB_ENV

    - name: Initialize OrangeFox source
      run: |
        mkdir -p $HOME/work
        cd $HOME/work
        repo init -u https://gitlab.com/OrangeFox/manifest.git -b $FOX_BRANCH --depth=1
        repo sync -c -j$(nproc) --no-tags --force-sync

    - name: Clone device tree
      run: |
        cd $HOME/work
        git clone --depth=1 $DT_LINK device/$OEM/$DEVICE

    - name: Sync dependencies
      run: |
        cd $HOME/work
        repo sync -c -j$(nproc) --no-tags --force-sync

    - name: Build recovery
      run: |
        cd $HOME/work
        source build/envsetup.sh
        lunch omni_${DEVICE}-eng
        export ALLOW_MISSING_DEPENDENCIES=true
        mka $TARGET -j$(nproc)

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: OrangeFox-${{ env.DEVICE }}
        path: |
          $HOME/work/out/target/product/${{ env.DEVICE }}/recovery.img
          $HOME/work/out/target/product/${{ env.DEVICE }}/*.zip
